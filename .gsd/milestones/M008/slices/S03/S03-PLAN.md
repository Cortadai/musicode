# S03: Flyway Migrations + Token Encryption

**Goal:** Replace ddl-auto:update with Flyway. Baseline current schema. Encrypt scrobble tokens in DB.
**Demo:** After this: After this: app starts with Flyway managing schema. Existing H2 data survives. Scrobble tokens encrypted at rest in DB.

## Tasks
