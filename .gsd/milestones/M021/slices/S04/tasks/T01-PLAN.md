---
estimated_steps: 1
estimated_files: 1
skills_used: []
---

# T01: Build PaletteSelector component with color swatches

Create a PaletteSelector component that shows all 9 palettes as selectable swatches grouped by dark/light. Each swatch shows the palette's accent color and label. Uses useTheme().setPalette() for selection. Active palette highlighted.

## Inputs

- `palettes registry from palettes/index.ts`
- `useTheme() hook with setPalette()`

## Expected Output

- `PaletteSelector component with dark/light groups and color swatches`

## Verification

Component renders all 9 palettes, clicking one changes the palette, active state visible
