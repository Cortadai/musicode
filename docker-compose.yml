services:
  caddy:
    build:
      context: .
      dockerfile: caddy/Dockerfile
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      sonance-server:
        condition: service_healthy

  sonance-server:
    build: ./sonance-server
    expose:
      - "8080"
    volumes:
      - ./sonance-server/data:/app/data
      - ${MUSIC_DIR:?MUSIC_DIR must be set in .env}:/music:ro
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SONANCE_COVERS_DIR=/app/data/covers
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

volumes:
  caddy_data:
  caddy_config:
